package Conditionals;

public class Main {
    public static void main(String[] args) {
        /*
            Syntax of if
            if (boolean exp T or F) {
                //body
            } else or else if {
                // do this
            }



             Syntax of FOR LOOP
             for (init; condition; inc/dec;) {
                // Body
             }


             Syntax of WHILE LOOP
             while (condition) {
                // BODY
             }




             Syntax of do while loop (diff btw while and do is do occurs atleast once coz checks after implmt)
             do {
                // BODY
             } while (cond);


         */
    }
}
